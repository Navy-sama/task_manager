# Behind a TLS-terminating proxy (Cloud Run): trust X-Forwarded-* headers.
server.forward-headers-strategy=framework
app.auth.cookie-secure=true

# API docs and Swagger UI are a discovery aid for local/dev work only: no endpoint to expose in prod.
springdoc.api-docs.enabled=false
springdoc.swagger-ui.enabled=false
