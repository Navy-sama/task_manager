spring.datasource.url=${DATABASE_URL:jdbc:mysql://localhost:3306/taskmanager}
spring.datasource.username=${DATABASE_USERNAME:taskmanager}

# Local HTTP: browsers would drop Secure cookies on plain http.
app.auth.cookie-secure=false

logging.level.io.github.navysama.taskmanager=DEBUG
