<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug-build only (lives under src/debug, not src/main, so it is never
  packaged into a release build). Allows plain HTTP only to the addresses
  used to reach a local backend during development (Android emulator host
  alias and localhost). All other destinations still require HTTPS.
  See mobile/README.md.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
